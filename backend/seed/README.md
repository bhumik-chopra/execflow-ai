# Dataset seeding (deferred)

This directory is reserved for the supplied assignment data and a future
one-command, repeatable MongoDB Atlas seed entry point. No data, collections,
or seed command are implemented in this scaffold.
